//2.4 鸡兔同笼.cpp
#include<iostream.h>
using namespace std;

int main()
{
    int a=0, b=0, c=0, x = 5, y = 3,z=1;//a为公鸡数b母鸡数c为小鸡数x公鸡单价y母鸡单价z为小鸡单价
    
    if (a * x + b * y + c * z != 100 || a + b + c != 100)
    {
        for(;c += 3;)
    }
    else
        cout << a <<" " << b << " " << c << endl;
    return 0;
}

